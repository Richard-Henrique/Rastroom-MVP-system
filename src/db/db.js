const DB_NAME = "rouba-dados"
const VERSION = 1
const STORE_NAME = "fase_inicial"

function openDB(){
    return new promisse((resolve,reject) => {
        const request = indexedDB
        request.
    })
}